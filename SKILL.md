---
name: project-organization
description: "项目文件组织与 AI 协作管理。当检测到 PROJECT.md 时自动加载；当用户明确表达项目管理意图时（如'初始化项目'、'需要项目管理'、'帮我整理项目'、'开个新项目'）也触发并主动询问。提供 .proj/ 目录结构、任务队列、待办事项、进度追踪、Git 标签和 GitHub 自动同步功能。触发条件：PROJECT.md 存在或用户明确要求项目管理/初始化项目/整理项目结构。"
---

# 项目文件组织规范

## 一、触发条件

### 1.1 精确触发（高优先级）

满足以下任一条件时，无条件激活：
- 工作目录或其父目录中存在 `PROJECT.md` 文件
- `.proj/` 目录已存在

### 1.2 意图触发（需明确表达）

用户**明确表达项目管理意图**时触发，主动询问是否初始化项目结构：

| 触发场景 | 示例语句 |
|---------|---------|
| 初始化意图 | "初始化个项目"、"开个新项目"、"创建个项目" |
| 管理意图 | "需要项目管理"、"帮我整理项目"、"管理一下这个项目" |
| 目录意图 | "这个目录是个项目"、"在这里建个项目结构" |

**不触发**（除非显式要求）：日常对话中的"项目"字眼、技术选型讨论、第三方项目提及。

### 1.3 询问话术

检测到意图但无 `PROJECT.md` 时：
```
检测到你可能需要项目管理支持。要为此目录初始化项目结构吗？

这将创建：
- PROJECT.md（项目身份证）
- .proj/ 目录（work/、archive/、temp/、scripts/）

输入 y 确认，n 跳过：
```

## 二、目录结构

```
项目根目录/
├── PROJECT.md              ← 项目身份证（唯一外露标记）
├── .proj/                  ← 统一收纳（隐藏目录）
│   ├── config.yaml        ← Git/GitHub 配置
│   ├── scripts/            ← 工具/辅助脚本
│   ├── archive/            ← 旧版本、已完成阶段归档
│   ├── temp/               ← 临时产物
│   └── work/               ← AI 协作文件
│       ├── task_plan.md    ← 任务队列
│       ├── todo.md         ← 待办事项
│       ├── progress.md     ← 会话进度
│       └── findings.md     ← 调研记录（可选）
└── (项目核心文件...)
```

| 路径 | 读写频率 | 清理策略 |
|------|---------|---------|
| `PROJECT.md` | 偶尔 | 不清理 |
| `.proj/config.yaml` | 偶尔 | 不清理 |
| `.proj/work/*` | 频繁 | 不清理 |
| `.proj/archive/` | 低频 | 手动清理 |
| `.proj/temp/` | 用完即删 | 会话结束清理 |
| `.proj/scripts/` | 按需 | 项目结束评估 |

## 三、进入项目（会话开始）

按顺序执行：

1. **读取 PROJECT.md** — 项目名称、根目录、当前状态
2. **检查/创建 .proj/** — 缺失子目录只增不减，不覆盖已有
3. **加载协作文件** — task_plan.md、todo.md、progress.md、findings.md
4. **加载 Git 配置** — 若 config.yaml 存在，读取远程仓库和同步设置
5. **GitHub 同步** — 若启用，执行 `git pull` 拉取远程更新
6. **构建上下文** — 将任务、待办、进度纳入工作记忆

> 旧结构兼容：若存在 `_归档/`、`_tmp/`，询问用户是否迁移。

### 3.1 跨终端接力读取（最高优先级）

> **强制规则**：适用于多终端、多会话窗口并行工作的项目。

会话总结的天然边界是**单个会话窗口**，但 git 推送的边界是**仓库级别**。当用户在多个终端并行工作时，单个会话的总结会**漏掉其他终端的进展**——本地 HEAD 与远程 HEAD 不一致，会话之间没有可靠的接力。

**解决方案：把总结写入 `.proj/work/progress.md`（已纳入 git 追踪），让所有终端共享一份接力文档。**

加载顺序调整（在第 3 步"加载协作文件"中优先）：

1. **`progress.md` 优先读取** — 这是其他终端留下的接力契约
2. 读取最后一段「接续点」，明确上次会话的 commit 列表、HEAD 状态、未完成事项
3. **`git fetch` 强制执行** — 即使没有 `auto_pull`，也要 fetch 一次
4. 对比 `HEAD` 与 `origin/main`：
   - 本地领先 → 本次会话的 commit 还没推
   - 远程领先 → 其他终端有进展 → **必须**读取 `.proj/work/` 内新增文件，必要时合并
   - 同步后在 `progress.md` 追加一段说明本次 fetch 行为
5. 接着按正常顺序加载 task_plan.md / todo.md / findings.md

## 四、项目初始化

### 4.1 初始化步骤

用户确认后依次执行：

1. 创建 `.proj/` 目录结构
2. 生成 `PROJECT.md`（使用 references/project-template.md）
3. 生成 `.proj/work/todo.md`（使用 references/todo-template.md）
4. 询问 Git/GitHub 配置

### 4.2 Git/GitHub 配置（一键式）

```
是否启用版本控制？（可多选）

[y] Git + GitHub（推荐）：自动标签 + 自动同步
[n] 仅本地：不启用远程同步

直接回车使用推荐配置，输入 n 跳过：
```

**推荐流程（选 y）**：
1. 检查是否为 Git 仓库 → 否时执行 `git init`
2. 询问 GitHub 仓库地址
3. 添加远程仓库并首次推送
4. 创建初始标签 `v0.1.0`
5. 生成 `.proj/config.yaml`

### 4.3 配置文件结构

```yaml
# .proj/config.yaml
git:
  enabled: true
  auto_tag: true
  tag_prefix: "v"
  last_tag: "v0.1.0"

github:
  enabled: true
  remote: "origin"
  repository: "https://github.com/user/repo.git"
  auto_push: true
  auto_pull: true

sync:
  session_count: 0     # 会话计数器
  sync_interval: 3      # 每 N 次会话强制同步
  last_sync: ""         # 上次同步时间
```

## 五、Git 与 GitHub 同步

### 5.1 智能版本标签

自动判断版本号递增级别：

| 变更类型 | 示例 | 递增 | 新版本 |
|---------|------|------|--------|
| patch | 修复 bug、文档更新、格式调整 | patch | v0.1.0 → v0.1.1 |
| minor | 新增功能、改进体验 | minor | v0.1.0 → v0.2.0 |
| major | 架构重构、breaking change | major | v0.1.0 → v1.0.0 |

**自动判断规则**：
- 纯文案/注释改动 → patch
- 新增文件（非脚手架）→ minor
- 删除核心模块、路径重构 → major

### 5.2 同步时机

| 时机 | 条件 | 行为 |
|------|------|------|
| 会话开始 | `auto_pull: true` | 自动 `git pull` |
| 会话结束 | `auto_push: true` | 自动 `add → commit → push` |
| 定期 | `session_count % sync_interval == 0` | 强制同步 |
| 重要里程碑 | 用户确认 | 立即推送 |

### 5.3 冲突处理

```
检测到远程更新与本地冲突：
1. 暂存本地更改 → git stash
2. 拉取远程 → git pull
3. 弹出暂存 → git stash pop
4. 若冲突，提示用户手动解决
```

### 5.4 故障恢复

| 故障类型 | 处理方式 |
|---------|---------|
| 网络超时 | 本地队列缓存，下次重试 |
| 推送被拒 | 检查分支状态，提示用户 |
| 仓库不存在 | 询问是否创建新仓库 |

## 六、协作行为

### 6.1 文件分类决策树

```
新建/移动文件时：
    └── 是项目核心交付物？
        ├── 是 → 根目录或核心目录（协商确认）
        └── 否 → 是工具脚本？ → .proj/scripts/
                是旧版本/备份？ → .proj/archive/
                是临时产物？ → .proj/temp/
                是 AI 协作文件？ → .proj/work/
                不确定？ → .proj/temp/（会话结束再确认）
```

> `node_modules/`、`package.json` 等环境依赖不纳入此体系。

### 6.2 任务队列生命周期

```
task_plan.md 全部 ✅
        ↓
① 归档：.proj/archive/task_plan_YYYY-MM-DD.md
        ↓
② 记录：本次已完成项写入 progress.md
        ↓
③ 升格：从 todo.md 提取未勾选且有价值的条目
        ↓
④ 新建：新的 task_plan.md
        ↓
⑤ 更新：PROJECT.md「当前任务」字段
        ↓
⑥ 同步：若启用 GitHub，自动提交推送
```

### 6.3 待办事项规则

| 对比项 | task_plan.md | todo.md |
|--------|-------------|---------|
| 粒度 | 粗（里程碑） | 细（具体项） |
| 顺序 | 有依赖 | 可并行 |
| 生命周期 | 阶段性 | 持续流动 |
| 本质 | "项目怎么走" | "今天做什么" |

**分区标签**：立即做（紧急）| 本周（近期）| 有空再说（暂缓）

**状态标记**：
- `- [ ]` 未开始
- `- [x]` 已完成（不删除）
- `- [~]` 已过时
- `- [?]` 待定（连续两阶段未处理）

**健康检查**（每 5 次会话）：
- 「有空再说」超过 10 条 → 建议清理
- 连续 3 次未提及 → 逐条询问保留/删除
- 分区模糊 → 建议合并重命名

## 七、会话结束

按顺序执行，**不可跳过**：

1. **更新 PROJECT.md** — 上次会话日期 + 当前任务
2. **标记任务队列** — 已完成项标记 ✅，全部完成时触发归档流程
3. **勾选待办** — 本次完成的条目 + 清理过时条目
4. **写入进度** — 日期 + 完成事项 + 待办 + todo 变动
5. **检查临时文件** — 列出 .proj/temp/ 内容，提示清理
6. **GitHub 同步** — 若启用且 auto_push: true，执行自动推送
7. **主动总结** — 用户发出暂停信号时立即执行 1-6

**暂停信号**：先这样、先写这么多、今天到这、暂停、总结、结束

### 7.1 跨终端接续点契约（强制）

执行第 4 步「写入进度」时，**必须**按以下结构追加到 `progress.md`：

```markdown
## 2026-07-24 会话 #N

**完成事项：**
- ✅ <本会话的 commit 列表 + 一句话描述>
- ✅ <重大决策记录>

**待办事项（接续点）：**
- [ ] <留给下次会话的事项 1>
- [ ] <留给下次会话的事项 2>

**本会话变更摘要：**
- <关键架构/文件变化 1-3 条>

**备注：**
- <跨终端同步状态、阻塞、决策记录>
```

**契约规则**：
- 每次会话结束**必须**追加一段，**不写视为会话不完整**
- 「接续点」段落是跨终端的**唯一接力契约**，下次会话开局必读
- 本次 commit + `progress.md` 追加**必须**一起 commit + push，让其他终端能接力
- 若本会话含跨终端同步（`git fetch` 后有 merge），需在备注中**显式标注**并列出合并的 commit

## 八、定期维护

| 周期 | 操作 |
|------|------|
| 每 5 次会话 | temp/ 和 archive/ 清理检查 |
| 每 5 次会话 | todo.md 健康检查 |
| 检测到散文件 | 询问是否归类 |
| 定期同步检查 | 超出门槛则强制同步 |

## 九、错误处理规范

### 9.1 Git 操作错误

| 错误 | 处理 |
|------|------|
| 非 Git 仓库 | 询问是否初始化 |
| 无远程仓库 | 询问是否添加 |
| 推送被拒 | 检查权限，提示解决 |
| 合并冲突 | 保留本地，执行 stash → pull → stash pop |
| 网络超时 | 写入 `pending_commits` 队列，下次重试 |

### 9.2 文件操作错误

| 错误 | 处理 |
|------|------|
| 目录创建失败 | 检查权限，提示手动创建 |
| 文件写入失败 | 保留内存，提示用户 |
| 模板不存在 | 使用内联默认模板 |

### 9.3 配置错误

| 错误 | 处理 |
|------|------|
| config.yaml 格式错误 | 使用默认配置，提示用户 |
| 远程仓库无效 | 询问重新配置 |
| 标签格式错误 | 强制使用 `v{major}.{minor}.{patch}` |

## 十、PROJECT.md 模板

```markdown
# <项目名称>

## 基本信息
- **根目录路径**：`<绝对路径>`
- **创建时间**：YYYY-MM-DD
- **上次会话**：YYYY-MM-DD
- **项目简介**：<一句话描述>

## 目录结构
| 路径 | 用途 |
|------|------|
| `<核心目录1>/` | <用途说明> |
| `.proj/scripts/` | 工具脚本 |
| `.proj/archive/` | 旧版本备份 |
| `.proj/temp/` | 临时产物 |
| `.proj/work/` | AI 协作文件 |

## 当前状态
- **当前任务**：<进行中的任务>
- **当前阶段**：<阶段名称>

## GitHub 同步
- **远程仓库**：<仓库地址>
- **上次同步**：YYYY-MM-DD
```

## 十一、边界与例外

- `.git/`、`.gitignore` 由 Git 管理，不归本规范
- 已有 `.proj/` 的项目兼容旧结构，只做增量
- 非项目目录不触发，除非用户明确要求
- 环境依赖按语言生态惯例处理
- GitHub 故障不阻塞用户操作，队列缓存待后续重试
