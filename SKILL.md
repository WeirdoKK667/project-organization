---
name: project-organization
description: "项目文件组织与 AI 协作管理。当检测到工作目录中存在 PROJECT.md 时自动加载。当用户提到'项目'、'需要项目管理'、'初始化项目'、'当前文件夹'、'这个目录'等关键词时，也应触发本技能并主动询问是否需要建立项目结构。管理 .proj/ 目录结构、文件分类归档、任务队列、待办事项、进度追踪、会话总结、Git 标签和 GitHub 自动同步。强烈建议在以下场景使用：创建新项目、规划任务、管理待办事项、记录进度、整理文件归档、或者任何涉及项目结构管理的工作。触发条件：PROJECT.md 存在于工作目录或其父目录中，或者用户提到项目/文件夹管理等关键词。"
---

# 项目文件组织规范

## 一、触发条件

本 skill 在以下情况自动触发：

1. **文件检测**：工作目录或其任意父目录中存在 `PROJECT.md` 文件
2. **关键词触发**：用户提到以下词汇时主动询问
   - "项目"、"项目结构"、"项目管理"
   - "初始化"、"开始个项目"
   - "当前文件夹"、"这个目录"、"这里是个项目"
   - "需要管理"、"帮我整理"

当检测到关键词但无 `PROJECT.md` 时，主动询问用户是否需要初始化项目结构。

## 二、目录结构

项目根目录遵循以下结构：

```
项目根目录/
├── PROJECT.md              ← 项目身份证（唯一外露标记）
├── .proj/                  ← 统一收纳（隐藏目录）
│   ├── config.yaml        ← 项目配置（Git/GitHub 设置）
│   ├── scripts/            ← 工具/辅助脚本
│   ├── archive/            ← 旧版本、备份、已完成阶段的归档（只读备查）
│   ├── temp/               ← 临时产物（定期清理）
│   └── work/               ← AI 协作文件（频繁读写）
│       ├── task_plan.md    ← 当前阶段的任务队列（路线图）
│       ├── todo.md         ← 待办事项清单（零散可执行项）
│       ├── progress.md     ← 历次会话进度记录
│       └── findings.md     ← 调研/发现记录（可选）
└── (项目核心文件...)       ← 自由组织
```

### 各目录属性

| 路径 | 生命周期 | 读写频率 | 清理策略 |
|------|---------|---------|---------|
| `PROJECT.md` | 项目全周期 | 偶尔更新 | 不清理 |
| `.proj/config.yaml` | 项目全周期 | 偶尔修改 | 不清理 |
| `.proj/scripts/` | 中期 | 按需使用 | 项目结束时评估 |
| `.proj/archive/` | 长期低频 | 只读备查 | 手动清理 |
| `.proj/temp/` | 短期 | 用完即删 | 每次会话结束时提示清理 |
| `.proj/work/` | 项目全周期 | 频繁读写 | 不清理 |

> `.proj/work/` 虽在隐藏目录下，但属于高频读写区。Claude 会在进入项目时自动读取其中文件，用户无需手动翻隐藏目录。

## 三、进入项目（会话开始时）

识别到项目后，按以下步骤初始化：

1. 读取 `PROJECT.md`，获取项目名称、根目录路径、目录结构、当前状态
2. 检查 `.proj/` 目录是否存在，若不存在则自动创建完整结构（scripts/、archive/、temp/、work/）
3. 读取 `.proj/work/task_plan.md`（任务队列）、`.proj/work/todo.md`（待办事项）和 `.proj/work/progress.md`（进度记录）
4. 若存在 `.proj/work/findings.md`，一并加载作为上下文
5. **检查 Git/GitHub 配置**：读取 `.proj/config.yaml`，若存在则加载远程仓库信息和同步设置
6. 将当前任务、待办事项和进度信息纳入工作记忆

> **兼容处理：** 若 `.proj/` 已存在但部分子目录缺失，只创建缺失的目录，不覆盖已有内容。若 `todo.md` 不存在，自动创建空模板。若项目使用旧版目录结构（如 `_归档/`、`_tmp/`），在用户同意的前提下帮助迁移。

## 四、项目初始化流程

当检测到关键词触发但无 `PROJECT.md` 时：

### 4.1 询问用户

主动询问用户：
```
检测到你可能需要项目管理支持。要为此目录初始化项目结构吗？

这将创建：
- PROJECT.md（项目身份证）
- .proj/ 目录（包含 work/、archive/、temp/、scripts/）

是否继续？ [Y/n]
```

### 4.2 初始化项目

用户确认后：
1. 创建 `.proj/` 目录结构
2. 创建 `PROJECT.md`（基于 `references/project-template.md`）
3. 创建 `.proj/work/todo.md`（基于 `references/todo-template.md`）
4. 询问是否启用 Git 和 GitHub 功能

### 4.3 Git/GitHub 功能询问

```
是否启用以下功能？（可多选）

[ ] Git 版本控制 - 自动标签管理（v1.0.0、v1.1.0 等）
[ ] GitHub 同步 - 自动推送更新到远程仓库

输入选项编号或直接输入 'y' 全部启用，'n' 跳过：
```

### 4.4 Git 配置流程

若用户选择启用 Git：
1. 检查是否已是 Git 仓库
2. 若不是，初始化 `git init`
3. 创建初始提交
4. 询问是否创建 GitHub 仓库并推送

### 4.5 GitHub 配置流程

若用户选择启用 GitHub：
1. 询问 GitHub 仓库地址（或让用户选择新建）
2. 添加远程仓库
3. 首次推送（main 分支）
4. 创建初始标签（如 `v0.1.0`）
5. 生成 `.proj/config.yaml` 保存配置

## 五、Git 与 GitHub 自动同步

### 5.1 配置存储

`.proj/config.yaml` 结构：
```yaml
git:
  enabled: true
  auto_tag: true
  tag_prefix: "v"
  last_tag: "v0.1.0"

github:
  enabled: true
  remote: "origin"
  repository: "https://github.com/user/repo.git"
  auto_push: true
  auto_pull: true

sync:
  auto_sync: true
  sync_interval: 3  # 每 N 次会话同步一次
  last_sync: "2026-07-24"
```

### 5.2 自动标签管理

当 `auto_tag: true` 时：
- 每次会话结束，若有实质性提交，自动更新标签
- 标签规则：`v{major}.{minor}.{patch}`
  - patch：日常小修复
  - minor：新增功能或重要改进
  - major：重大架构变更

### 5.3 自动推送

当 `auto_push: true` 时：
- **会话结束时**：自动推送所有更改
- **有重要里程碑**：询问是否立即推送
- **定期自动推送**：每 N 次会话自动执行

### 5.4 自动拉取更新

当 `auto_pull: true` 时：
- **每次会话开始**：自动拉取远程更新
- **检测到冲突**：提醒用户手动解决

### 5.5 同步检查点

以下情况触发同步检查：
1. 会话开始时（自动拉取）
2. 完成重要任务时（询问是否推送）
3. 会话结束时（自动推送）
4. 定期检查（每 N 次会话）

## 六、协作中的行为

### 6.1 文件分类

创建或写入文件时，按以下决策树自动归类：

```
文件是否为项目核心交付物？
├── 是 → 放在根目录或核心目录（与用户协商确定）
└── 否 →
    ├── 是一次性工具脚本？     → .proj/scripts/
    ├── 是旧版本/备份？        → .proj/archive/
    ├── 是临时产物/Demo？      → .proj/temp/
    ├── 是 AI 协作文件？       → .proj/work/
    └── 不确定？              → .proj/temp/（会话结束时再确认）
```

- **环境依赖**（`node_modules/`、`package.json`、`requirements.txt` 等）不纳入此体系，按语言生态惯例处理
- **AI 协作文件** 包括：任务计划、待办事项、进度记录、会话笔记、发现/调研记录

### 6.2 任务队列管理

依据 `.proj/work/task_plan.md` 中的任务队列进行工作：

- 每完成一个大步骤，**立即**在队列文件中将该步骤标记为已完成（✅）
- 新增任务时，追加到队列末尾并标注状态
- 确保下次恢复时进度一目了然

#### 任务队列生命周期

任务队列不是永久存在的——一个阶段的队列全部 ✅ 即意味着一个里程碑达成，应触发闭环：

```
task_plan.md 全部 ✅
        ↓
① 归档到 .proj/archive/task_plan_YYYY-MM-DD.md（保留历史，日后可回溯）
        ↓
② 检查 todo.md：已勾选的随本次归档记录在 progress.md 中
        ↓
③ 新建 task_plan.md：从 todo.md 中提取未勾选但有价值的条目，升格为下一阶段任务
        ↓
④ 更新 PROJECT.md「当前任务」字段，指向新阶段目标
        ↓
⑤ 若 GitHub 已启用，自动提交并推送更新
```

**归档命名规则：** `.proj/archive/task_plan_YYYY-MM-DD.md`，日期取归档当天。

**归档不等于删除：** 历史队列文件保留在 archive 中，只读备查——"那个阶段做了什么"一目了然。

### 6.3 状态同步

在协作过程中，保持 `PROJECT.md` 的「当前状态」区与实际情况同步。

### 6.4 待办事项管理

`todo.md` 与 `task_plan.md` 互补：

| | `task_plan.md` | `todo.md` |
|---|---|---|
| **粒度** | 粗——里程碑、大步骤 | 细——具体可执行项 |
| **顺序** | 有序、有前后依赖 | 可并行、无严格顺序 |
| **生命周期** | 稳定，阶段结束时才变 | 流动，随时增删勾销 |
| **来源** | 用户规划的项目路线图 | 协作中随时冒出来的"要做/想到的事" |
| **本质** | 回答「项目怎么走」 | 回答「今天/这周做什么」 |

#### 格式规范

```markdown
# 待办事项

## 立即做
- [ ] 修复登录页样式错乱                                  #bug @登录模块
- [ ] 补充用户模块单元测试                                #test

## 本周
- [ ] 重构 API 层错误处理                                 #refactor
- [ ] 调研 Zustand vs Redux 选型                          #调研

## 有空再说
- [ ] 给后台管理加暗色模式                                #enhancement
- [ ] 写一篇项目架构文档                                  #docs
```

- `- [ ]` / `- [x]`：Markdown 原生勾选框，Git diff 友好
- `#标签`：自由分类（如 `#bug` `#test` `#refactor` `#调研` `#enhancement` `#docs`）
- `@模块`：关联到项目具体模块（可选）
- 分区：按紧急程度分组（「立即做」「本周」「有空再说」），可根据项目实际调整分区名称

#### 新增条目

会话中任何时刻，用户提到"要做的/该做的/别忘了/回头看看"的事项时，**主动询问是否加入 `todo.md`**。新增时：

1. 判断紧急程度，放入对应分区
2. 补上合适的 `#标签`
3. 如果该 todo 与当前 `task_plan.md` 中某项关联，可追加 `→ task_plan 第N项` 标注

```
- [ ] 补充用户模块单元测试                                #test → task_plan 第3项
```

#### 完成与勾选

- 条目完成后，将 `- [ ]` 改为 `- [x]`，**不删除**——保留勾选记录便于回顾
- 若某条目连续两个阶段都未勾选且未再被提及，标记为 `- [?]` 表示待定

#### 去旧与清理

以下情况触发条目清理：

| 触发条件 | 操作 |
|----------|------|
| 条目已过时（需求变更、不再适用） | 改为 `- [~] 原始内容` 并追加 `~~已过时：原因~~`，保留记录 |
| 条目已融入 `task_plan.md` | 追加标注 `→ 已升格至 task_plan`，保留原条目勾选 |
| 条目在「有空再说」区躺了超过 5 次会话 | 主动提醒用户：保留 / 删除 / 降级为「暂缓」 |
| 「立即做」分区为空 | 提示用户是否从「本周」或「有空再说」中提升条目 |

**去旧不等于删除：** 优先使用标记（`[~]`、`~~划掉~~`）而非直接删除，保持历史可追溯。

#### 升格机制

每个阶段结束时（`task_plan.md` 全部 ✅），执行升格：

1. 遍历 `todo.md` 中未勾选的条目
2. 识别其中**适合作为下一阶段任务**的条目（有明确产出、有一定工作量）
3. 与用户确认后，将这些条目写入新的 `task_plan.md`
4. 在原 `todo.md` 条目后追加 `→ 已升格至 task_plan YYYY-MM-DD`

#### 定期审查

每约 5 次会话，主动对 `todo.md` 做一次健康检查：

- 「有空再说」区是否积压严重（超过 10 条）→ 建议清理或移入独立「暂缓池」
- 是否存在连续 3 次会话未被提及的条目 → 逐条询问保留/删除
- 分区边界是否模糊 → 建议合并或重命名分区

## 七、会话结束时的行为

1. **更新 PROJECT.md：** 更新「上次会话」日期和「当前任务」字段
2. **更新任务队列：** 在 `.proj/work/task_plan.md` 中标记所有已完成项为 ✅。若全部完成，询问用户是否现在执行归档与升格流程（见 6.2 节），不要等到下次会话
3. **更新待办事项：** 在 `.proj/work/todo.md` 中勾选本次会话已完成的条目；清理已过时条目
4. **写入进度小结：** 在 `.proj/work/progress.md` 中追加本次会话的工作总结（日期 + 完成事项 + 待办事项 + todo 变动摘要）
5. **检查临时文件：** 列出 `.proj/temp/` 中的内容，提醒用户清理不再需要的文件
6. **GitHub 自动同步：** 若 `.proj/config.yaml` 中 `auto_push: true`，自动执行 `git add . && git commit && git push`
7. **主动总结：** 当用户发出暂停信号（如"先写这么多吧""今天到这""暂停""总结一下""结束"等），不要等用户提醒，立即执行本节的 1-6 步

## 八、定期维护

- 每约 5 次会话后，主动检查 `.proj/temp/` 和 `.proj/archive/`，提醒用户是否需要清理或重新评估分类
- 每约 5 次会话后，对 `todo.md` 执行健康检查（见 6.4 节「定期审查」）
- 检测到项目根目录中出现未分类的松散文件时，主动询问是否归入 `.proj/` 相应子目录
- **定期同步检查**：若 GitHub 已启用，检查距上次同步是否已超过 `sync_interval` 设置，若超出门槛则自动执行同步

## 九、PROJECT.md 模板

新项目初始化时，按以下模板创建 `PROJECT.md`：

```markdown
# <项目名称>

## 基本信息
- **根目录路径**：`<绝对路径>`
- **创建时间**：YYYY-MM-DD
- **上次会话**：YYYY-MM-DD
- **项目简介**：<一句话描述>

## 目录结构
| 路径 | 用途 |
|------|------|
| `<核心目录1>/` | <用途说明> |
| `<核心目录2>/` | <用途说明> |
| `.proj/scripts/` | 工具脚本 |
| `.proj/archive/` | 旧版本与备份 |
| `.proj/temp/` | 临时产物（定期清理） |
| `.proj/work/` | AI 协作工作文件（频繁读写） |

## 当前状态
- **当前任务**：<进行中的任务简述>
- **当前阶段**：<阶段名称>

## GitHub 同步
- **远程仓库**：<仓库地址>
- **上次同步**：YYYY-MM-DD
```

### 字段要求

- **根目录路径**：必须为绝对路径
- **目录结构**：只列核心产出目录，`.proj/` 子目录固定不变
- **当前状态**：由 Claude 在每次会话结束时自动更新

## 十、边界与例外

- **Git 仓库：** `.git/`、`.gitignore` 不归本规范管理，各司其职
- **已有 .proj/ 的项目：** 兼容已有结构，不强制覆盖，只做增量补充
- **非项目目录：** 无 `PROJECT.md` 的目录不触发本规范，除非检测到关键词
- **环境依赖：** `node_modules/`、`package.json` 等按语言生态惯例处理
- **GitHub 连接失败：** 若推送失败，保存到本地队列，下次会话重试，不阻塞用户
